{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "edge",
            "request": "launch",
            "version": "beta",
            "name": "Edge",
            "url": "http://localhost:3000",
            "webRoot": "${workspaceFolder}"
        }
    ]
}