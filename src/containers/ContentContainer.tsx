import * as React from "react";
import {useEffect} from "react";

/* For displaying content such as news only for reading/watching. */
const ContentContainer = () => {
    useEffect(() => {

    });

    return (
        <>

        </>
    )
};