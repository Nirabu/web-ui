import * as React from 'react';
import {WeatherType} from "./Categories";

interface SituationProps {
    type: WeatherType;
}

const Situation: React.FC = () => {
    return (
        <div>

        </div>
    )
};
export default Situation;